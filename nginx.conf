server {
    listen 80;

    location / {
        root /usr/share/nginx/html/main_device;
        try_files $uri $uri/ /index.html;
    }

    location /controller/ {
        alias /usr/share/nginx/html/controller/;
        try_files $uri $uri/ /index.html;
    }
}
